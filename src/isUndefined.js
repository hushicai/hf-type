/**
 * @file isUndefined
 * @author hushicai(bluthcy@gmail.com)
 */

define(
    function (require) {
        function isUndefined(val) {
            return val === undefined;
        }

        return isUndefined;
    }
);
