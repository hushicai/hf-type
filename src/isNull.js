/**
 * @file isNull
 * @author hushicai(bluthcy@gmail.com)
 */

define(
    function (require) {
        function isNull(val) {
            return val === null;
        }

        return isNull;
    }
);
